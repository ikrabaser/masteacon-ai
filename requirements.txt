fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
SQLAlchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3
pgvector==0.3.4
openai==1.109.1
python-multipart==0.0.9
pypdf==5.0.1
python-docx==1.1.2
tiktoken==0.8.0
python-dotenv==1.0.1
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
aiosqlite==0.20.0
bcrypt==4.2.0
PyJWT==2.9.0
email-validator==2.2.0
anthropic==0.122.0
celery[redis]==5.4.0
redis==5.0.8

# RAG pipeline orchestration: chunking, embeddings, chat models and the
# vector-store/retriever interface are all expressed through LangChain.
langchain==0.3.30
langchain-core==0.3.86
langchain-openai==0.2.14
langchain-anthropic==0.3.22
langchain-text-splitters==0.3.11

# Optional reranking (RERANKER_PROVIDER=cross_encoder): a local
# sentence-transformers cross-encoder model. Pulls in PyTorch as a transitive
# dependency - meaningfully larger install than the rest of this project, but
# only actually loaded into memory if cross-encoder reranking is used
# (RERANK_ENABLED=true + RERANKER_PROVIDER=cross_encoder); the default
# RERANKER_PROVIDER=lexical never imports it.
sentence-transformers==6.0.0
